export enum Themes {
  system = 'system',
  dark = 'dark',
  light = 'light'
}

export type AvailableLanguage = {
  code: string
  title: string
  currency: string
  short: string
  timeFormat: string
}
